0. web.xml 수정.{
	0-1. context-param> param-value =
		<param-value>classpath*:/config/spring/context/context-*.xml</param-value>
	0-2. filter 삽입.
		<!-- filter -->
		<filter>
			<filter-name>characterEncodingFilter</filter-name>
			<filter-class>org.springframework.web.filter.CharacterEncodingFilter</filter-class>
			<init-param>
				<param-name>encoding</param-name>
				<param-value>utf-8</param-value>
			</init-param>
		</filter>
		<filter-mapping>
			<filter-name>characterEncodingFilter</filter-name>
			<url-pattern>*.do</url-pattern>
		</filter-mapping>
	0-3. servlet> init-param> param-value =
 		<param-value>classpath:/config/spring/servlet/servlet-context.xml</param-value>
}

1. servlet-context.xml -> src/main/resources/config.spring.servlet 로 이동.
2. root-context.xml 삭제.
3. src 오버라이드.
4. vo 생성.
5. mybatis.mapper 설정.
6. DAO 생성.{
	SqlSession sqlSession;

	public SqlSession getSqlSession() {
		return sqlSession;
	}

	public void setSqlSession(SqlSession sqlSession) {
		this.sqlSession = sqlSession;	
	}
	
	public List<???Vo> selectList() {
		List<???Vo> list= null;
		list = sqlSession.selectList("???.???_list");
		return list;
	}
}
7. mybatis-config 에 mapper 추가.
8. service 생성.{
	//???Dao ???_dao;
	
	// create getter and setter;
	
	public List<???Vo> selectList() {
		List<???Vo> list= null;
		list = ???_dao.selectList();
		return list;
	}
}
9. context-3-???.xml 수정.
10. context-4-service.xml 수정.
11. servlet-context.xml 수정.{
	<!-- @AutoWired 속성 -->
	<context:annotation-config/>

	<!--
	auto-detecting 대신
	수동 생성.
	-->
	<beans:bean class="com.night_owl.package.Controller">
		<beans:property name="???" ref="???_service"/>
	</beans:bean>
}